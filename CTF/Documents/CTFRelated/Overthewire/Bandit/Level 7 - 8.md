The password for level 7: morbNTDkSW6jIlUc0ymOdMaLnOlFVAaj
![[Pasted image 20260429213535.png|579]]
We execute the following command
![[Pasted image 20260429213602.png]]
the **|** is the pipe, which redirects the output (or whatever get sends back from the command right before the pipe) to the next command, in this case, we direct the **cat data.txt** (which gives all of the content inside the file data.txt) into the command **grep "millionth"**.
You can read more using **man grep**
We can now move on to level 8