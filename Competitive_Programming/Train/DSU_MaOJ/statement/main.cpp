#include <bits/stdc++.h>
using namespace std;
const int N = 1e5 + 5;
int n, q;

int main(){
    ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);

    cin >> n >> q;

    while(q--){
        int l, r, t; cin >> l >> r >> t;

        
    }
}